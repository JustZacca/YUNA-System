# ===========================================
# YUNA-System Environment Configuration
# ===========================================
# Questo file serve SOLO per esecuzione locale (senza Docker)
# Con Docker, configura tutto in docker-compose.yml

# ------------------------------------------
# Telegram (obbligatorio)
# ------------------------------------------
TELEGRAM_TOKEN=your_telegram_bot_token_here
CHAT_ID=123456789

# ------------------------------------------
# Cartelle download (solo per uso locale)
# ------------------------------------------
# Con Docker questi path sono gestiti dai volumi in docker-compose.yml
DESTINATION_FOLDER=/path/to/your/anime
SERIES_FOLDER=/path/to/your/series
MOVIES_FOLDER=/path/to/your/movies

# ------------------------------------------
# Database (opzionale)
# ------------------------------------------
# Default: /data/yuna.db (Docker) o ./yuna.db (locale)
# DATABASE_PATH=/path/to/yuna.db

# ------------------------------------------
# Jellyfin (opzionale)
# ------------------------------------------
# JELLYFIN_URL=http://your-jellyfin:8096
# JELLYFIN_API_KEY=your_api_key

# ------------------------------------------
# Intervallo aggiornamento (opzionale)
# ------------------------------------------
# Tempo in minuti tra i controlli (default: 60)
# UPDATE_TIME=60
